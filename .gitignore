# Xcode / SwiftPM user- and machine-specific state — safe to ignore, regenerated locally.
xcuserdata/
*.xcuserstate
DerivedData/
.build/
.swiftpm/
*.ipa
*.dSYM.zip
*.dSYM

# Package.resolved is intentionally NOT ignored here — for an app (as opposed to a
# library), committing it pins the exact SwiftSoup version everyone builds against.

.DS_Store

# local maintenance guide (machine-specific paths — auto-loads locally, don't publish)
CLAUDE.md
# local tooling / runtime junk
.claude/
.wrangler/
*.log
